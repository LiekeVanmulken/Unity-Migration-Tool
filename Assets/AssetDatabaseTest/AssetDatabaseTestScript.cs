using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class AssetDatabaseTestScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        AssetDatabaseTest();
    }

    private void AssetDatabaseTest()
    {
//             
//        string dllGuid = null;
//        Object[] assemblyObjects = AssetDatabase.LoadAllAssetsAtPath(assemblyPath.GetUnityProjectRelativePath());
//     
//        for (int i = 0; i < assemblyObjects.Length; i++)
//        {
//            long dllFileId;
//     
//            if (AssetDatabase.TryGetGUIDAndLocalFileIdentifier(assemblyObjects[i], out dllGuid, out dllFileId))
//            {
//                Debug.Log(assemblyObjects[i].name + " " + dllFileId.ToString());
//            }
//        }


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
